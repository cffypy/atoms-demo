# 依赖 / 密钥
node_modules/
.env
.env.local
*.log

# 系统
.DS_Store
Thumbs.db

# 测试台（本地临时）
/tmp/test_gen.js

# 说明：secrets.js 已内置演示用 DeepSeek Key 并随仓库公开部署（纯静态站点
# 无后端，需前端直连）。生产环境请勿将真实 Key 提交到前端，改用后端代理或
# GitHub Repo Secret + Actions 注入。本地调试可保留 secrets.example.js 模板。
